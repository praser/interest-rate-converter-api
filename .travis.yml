language: node_js
node_js:
  - 11.9.0

# include the docker service so that we can user the docker command in the deployment script
services:
  - docker


deploy:
  provider: script
  script: bash src/scripts/deploy.sh
  on:
    branch: master